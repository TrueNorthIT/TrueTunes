{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window during migration",
  "windows": ["main", "mini", "login"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "updater:default"
  ]
}
